import pygame
pygame.font.init()

# initialize font; must be called after 'pygame.init()' to avoid 'Font not Initialized' error
FONT_MONOSPACE = pygame.font.SysFont("monospace", 15)
FONT_BOARD_COORDINATES = pygame.font.SysFont("cambria", 30)
